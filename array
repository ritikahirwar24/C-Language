#include <stdio.h>

int main() {
    int arr[] = {1, 2, -3, -1, -1};
    int size = sizeof(arr) / sizeof(arr[0]);
    int i, sum = 0;

    for (i = 0; i < size; i++) {
        sum += arr[i];
    }

    printf("Sum of array elements = %d\n", sum);

    return 0;
}